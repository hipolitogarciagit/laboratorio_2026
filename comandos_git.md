CD "carpeta que quieroo ver"= cambiar directorio
MKDIR= crea carpeta nueva dentro de otra carpeta
LS= VER CONTENIDO DEL DIRECTORIO
CD .. =VUELVE A LAS CARPETAS ANTERIOR
rmdir "nombre del directorio"= elimina directorio
git config --global user.name "crea usuario" 
git config --global user.email "crea un email"
git config --global init.defaultBranch "cambia la rama inicial"
git config --global core.editor "code --wait"= cabia el editor de cdigo asociado a git
git init =crea un repocitorio
git status=verifica el estatus del repocitorio
git add "nombre del archivo"=agrega un archivo
git rm --cached "nombre del archivo"=remueve el achivo
git commit "registra loos cambios en editor de codigo" 
git commit -m "mensaje"=registra los cambios
git commit --amend= cambia el texto del anterior commit "solo hacer en repositrio de mi pc"
git log= muestra los commits que ya hice
git reset --soft HEAD~1 =deshacer el ultimo commit
git branch version-nombre =crea una rama nueva
git branch= ver rama creada
git checkout version-nombre= ir a la rama creada
@